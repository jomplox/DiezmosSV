node_modules/
dist/
.wrangler/
.worktrees/
/tmp/
.dev.vars
.dev.vars.*
# .dev.vars.ci holds committed NON-SECRET demo values used by the CI Playwright
# smoke test; keep it tracked even though the glob above ignores .dev.vars.*.
!.dev.vars.ci
!.dev.vars.example
.DS_Store

# Local forbidden-host list for scripts/check-private-boundary.mjs. Never commit it.
.private-boundary-hosts

# Playwright e2e artifacts.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/e2e/.auth/

# Never publish real credentials, private keys, live certificates, or local MH exports.
DTE/Credentials/

# Keep the required JSON schemas, but do not publish bulky/private MH bundles.
DTE/dte-firmador/
DTE/svfe-api-firmador/
DTE/**/*.pdf
DTE/**/*.xlsx
DTE/**/*.csv
DTE/**/*_OCR.md
DTE/**/*_by_PaddleOCR*.md

# Local/private samples and duplicated working docs.
WompiWebhookSample.json
PRD_CDE_DTE_Wompi_ElSalvador*.md
GOAL.md
examples/**/DTE-*.json
examples/**/DTE-*.pdf
.claude/worktrees/
