# Stripe PDF Logo and Provenance Correction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the U.S. immediate-gift receipt and year-end statement demonstrably originate from the production render/delivery paths, populate every displayed field from durable gift or deployment evidence, and use the approved Elim and FMCE artwork at crisp, readable sizes on U.S. Letter pages.

**Architecture:** Keep the existing `pdf-lib` renderers and immutable evidence flow. Strengthen integration tests at the email-provider boundary so they extract the actual attached PDFs produced by the production services. Pin the Elim artwork to the immediate receipt and the exact owner-supplied FMCE PNG to the annual statement, rather than accepting a runtime branding substitution for these legal documents.

**Tech Stack:** TypeScript, Cloudflare Workers, D1/SQLite, `pdf-lib`, Vitest, Poppler (`pdftotext`, `pdftoppm`, `pdfinfo`).

## Global Constraints

- Both PDFs use U.S. Letter pages: exactly `612 x 792` PDF points (`8.5 x 11` inches), never A4.
- The immediate receipt uses the approved Elim mark already carried in the repository's Stripe PDF asset source, rendered substantially larger and with preserved aspect ratio.
- The annual statement uses the exact bytes of owner-supplied `FMCE_Brand.png`; its SHA-256 is `ac235e246a9d15381b32501f49eec7e8f8fb60a52214e0fde9a6595e5c67e19c` and its intrinsic dimensions are `2393 x 672`.
- The tracked `Sources/Logo.svg` is explicitly a neutral placeholder and must not replace either approved U.S. legal-document mark.
- A runtime branding upload must not silently substitute another logo in the U.S. annual legal statement.
- Every donor, gift, legal, contact, signer, date, identifier, and total visible in the PDFs must come from the production service's durable gift/snapshot/configuration inputs; no preview-only field path may exist.
- Previews must be generated by importing the production `renderStripeAcknowledgmentPdf` and `renderStripeAnnualStatementPdf` functions.
- Preserve the strict processor boundary: Stripe remains the U.S. 501(c)(3) lane; Wompi/MH/DTE behavior is untouched.
- Donor-facing copy uses donation/gift language and `usted`; forbidden purchase/customer/payment vocabulary must not be introduced.
- Existing uncommitted email-template separation and Exportar pagination changes are baseline work and must remain intact.
- Applied migrations `0032` through `0038` are immutable; migration `0039` remains append-only.
- Write a failing behavioral test first and observe the expected failure before each production change.

---

### Task 1: Prove Production Field Wiring and Preview Provenance

**Files:**
- Modify: `test/worker/stripeAcknowledgment.test.ts`
- Modify: `test/worker/stripeAnnualStatement.test.ts`
- Create: `scripts/render-stripe-us-pdf-previews.ts`
- Create: `test/scripts/stripePdfPreviews.test.ts`

**Interfaces:**
- Consumes: `deliverNextStripeAcknowledgment(env, repo, options)`, `sendStripeAnnualStatements(env, repo, year, livemode, actorId, request)`, `renderStripeAcknowledgmentPdf(input)`, and `renderStripeAnnualStatementPdf(input)`.
- Produces: `renderStripeUsPdfPreviews(outputDirectory: string): Promise<{ receiptPath: string; annualPath: string }>` which generates stable synthetic preview PDFs solely through the production renderers.

- [ ] **Step 1: Write failing service-boundary field-wiring tests**

  Extend the immediate acknowledgment delivery test so its real attachment is passed to `pdftotext -layout` and asserts hand-written sentinel values originating separately from the seeded D1 gift and Stripe U.S. deployment configuration: donor name, gross/net amount, gift type, frequency, source identifier, settled date, legal name, EIN, organization name, support email, phone, mailing address, signer name, and signer title.

  Extend the annual-send test the same way: seed two distinguishable D1 gift rows plus donor phone/address, intercept only the external email provider, extract the real attached PDF text, and assert donor identity/contact, both source IDs, row amounts/dates, computed net total/count, legal name, EIN, organization phone/site/address, statement number/year, and page count. The test must exercise `sendStripeAnnualStatements`, not call the renderer directly.

- [ ] **Step 2: Run the two focused tests and capture RED**

  Run:

  ```sh
  rtk npx vitest run test/worker/stripeAcknowledgment.test.ts test/worker/stripeAnnualStatement.test.ts -t "wires durable fields into the attached"
  ```

  Expected: fail because the existing provider-boundary tests do not yet assert all production-originated PDF fields (or because a real missing field is exposed).

- [ ] **Step 3: Add the production-renderer preview command and its behavioral test**

  Implement `renderStripeUsPdfPreviews(outputDirectory)` with synthetic, unmistakable sample values. It must call the exported production renderers directly and write `stripe-us-single-gift-receipt.pdf` and `stripe-us-annual-giving-statement.pdf`. The test invokes the function in a temporary directory, checks `%PDF`, extracts text with Poppler, verifies unique sample values in both files, and confirms page counts and `612 x 792` point media boxes through `PDFDocument.load`.

- [ ] **Step 4: Run the focused Task 1 suite and capture GREEN**

  Run:

  ```sh
  rtk npx vitest run test/worker/stripeAcknowledgment.test.ts test/worker/stripeAnnualStatement.test.ts test/scripts/stripePdfPreviews.test.ts
  ```

  Expected: all tests pass. Record exact counts in the task report.

- [ ] **Step 5: Self-review and commit**

  Confirm the email provider alone is mocked, the production service/snapshot/renderer paths remain real, expected values are literal and independent, and the preview command has no deployment or provider side effects. Commit with subject:

  ```text
  test: prove Stripe PDF production wiring
  ```

---

### Task 2: Correct Legal-Document Logos, Scale, and Letter Rendering

**Files:**
- Modify: `src/worker/services/stripePdfAssets.ts`
- Modify: `src/worker/services/stripeAcknowledgment.ts`
- Modify: `src/worker/services/stripeAnnualStatement.ts`
- Modify: `test/worker/stripeAcknowledgment.test.ts`
- Modify: `test/worker/stripeAnnualStatement.test.ts`
- Modify if necessary: `test/scripts/stripePdfPreviews.test.ts`

**Interfaces:**
- Consumes: the Task 1 production preview command and existing render inputs.
- Produces: two immutable PNG byte exports with explicit names and SHA-256 constants, a larger aspect-ratio-preserving Elim receipt header, and a fixed FMCE annual-statement header.

- [ ] **Step 1: Write failing asset, sizing, and media-box tests**

  Add literal assertions that:

  - the annual embedded PNG has SHA-256 `ac235e246a9d15381b32501f49eec7e8f8fb60a52214e0fde9a6595e5c67e19c` and intrinsic dimensions `2393 x 672`;
  - the receipt asset is the repository-carried Elim mark (not the old narrow Friends/FMCE mark and not `Sources/Logo.svg`);
  - the receipt `drawImage` call preserves the Elim asset's aspect ratio and occupies at least `180` points of width and `60` points of height without overlapping the salutation;
  - the annual `drawImage` call preserves the supplied FMCE asset's aspect ratio and is large enough to read while remaining left of the title;
  - both renderers create only `612 x 792` point pages;
  - passing a configured runtime raster to the annual renderer cannot substitute the fixed FMCE legal-statement mark.

- [ ] **Step 2: Run focused asset/render tests and capture RED**

  Run:

  ```sh
  rtk npx vitest run test/worker/stripeAcknowledgment.test.ts test/worker/stripeAnnualStatement.test.ts test/scripts/stripePdfPreviews.test.ts -t "approved logo|U.S. Letter"
  ```

  Expected: fail on the current swapped/low-resolution assets, small dimensions, and runtime annual-logo override.

- [ ] **Step 3: Implement the minimal asset and renderer correction**

  Replace the confusing generic asset exports with explicit receipt-Elim and annual-FMCE exports. Embed the exact owner-supplied FMCE PNG bytes without recompression. Reuse the existing approved Elim PNG bytes from the repository source for the receipt, preserving aspect ratio. Increase the receipt header slot and move the salutation down only as much as needed to guarantee clearance. Fit the FMCE annual mark into a wider header slot without colliding with the right-aligned statement title. Remove the annual renderer's runtime logo substitution path while retaining email branding separately. Bump renderer evidence versions so immutable snapshots identify the changed art; continue accepting older immediate-receipt evidence versions on retry.

- [ ] **Step 4: Run focused tests and mutation checks**

  Run the Task 2 focused suite. Then temporarily restore the old asset mapping/sizing and confirm at least one receipt and one annual test fail for the intended reasons; restore the fix and rerun GREEN.

- [ ] **Step 5: Generate production previews and visually inspect every page**

  Run the Task 1 preview command to `output/pdf/`, render every page with `pdftoppm -png -r 150`, inspect all PNGs, and confirm: no garbling, sharp logos, no overlap, no clipping, legible legal/contact blocks, correct table continuation, and correct page numbering. Use `pdfinfo` to confirm U.S. Letter page size and `pdftotext -layout` to confirm all synthetic fields.

- [ ] **Step 6: Run project verification**

  Run:

  ```sh
  rtk npm test
  rtk npm run build
  rtk npm run typecheck
  rtk npm run types:check
  rtk npm run security:check-private-boundary
  rtk npm run migrations:check-immutability
  rtk git diff --check
  ```

  Confirm migrations `0032` through `0038` have no diff and the donor-language guard remains green. Run the complete fresh-D1 Playwright suite only after stopping the owner service on port `8787`, using `DIEZMOSSV_ENV_FILE=.dev.vars.ci` and a new `PW_PERSIST_TO`, then restore the exact owner service and verify HTTP 200.

- [ ] **Step 7: Self-review and commit**

  Review only the Task 2 diff, record exact visual and automated evidence, and commit with subject:

  ```text
  fix: correct Stripe PDF legal-document logos
  ```
